# 設計思維入門：從 MedQA 學 OOA / OOD / Strategy Pattern

> 寫給自己的學習筆記。目標：不靠直覺寫程式，而是有一套分析框架。

---

## 這份筆記在說什麼？

你有沒有遇過這種情況：

- 程式跑起來了，但改 A 就壞 B
- 加一個新功能，要改五個地方
- AI 幫你生了程式碼，但你說不清楚為什麼這樣設計

這些問題的根源，**不是技術不夠好，而是在還沒想清楚「需求的結構」之前就動手了**。

這份筆記的框架就是來解決這件事的。

---

## 核心框架：四個步驟，缺一不可

```
OOA          Forces        OOD           OOP
需求結構化  →  找出衝突  →  套用 Pattern  →  照藍圖實作
```

> **先搞懂一個詞：Pattern（設計模式）＝ 前人驗證過的「解題範本」**
>
> Pattern 不是某段具體程式碼，而是「針對某一類**常見**設計問題，前人歸納出來的**標準解法**」。
> 就像蓋房子時，「承重牆怎麼配置」「樓梯怎麼設計」每棟房子都會遇到，建築師不會每次重新發明，而是套用驗證過的結構範式——Pattern 就是軟體界的這種範本。
>
> 1990 年代四位作者（GoF, Gang of Four）整理出 23 個經典 Pattern，變成軟體設計的共通語言。
> **你的工作不是發明 Pattern，而是認出「我這個 Forces 衝突，對應到哪個 Pattern」，然後套用。**
> 這也是為什麼框架順序是 Forces → OOD（套 Pattern），不能反過來——沒有衝突，就不需要範本。

---

## Step 1｜OOA（Object-Oriented Analysis｜物件導向分析）

### 白話說明

在動手寫任何程式之前，先問自己：

> **「這個功能裡，有哪幾件事情會獨立地發生變化？」**

「獨立變化」是關鍵詞。如果 A 改了，B 不需要跟著改，那 A 和 B 就是兩個獨立的維度。

### MedQA 的例子

MedQA 的需求看起來很簡單：「使用者問問題，系統給答案」。但拆開來看：

| 獨立變化的維度 | 說明 |
|---|---|
| **問題分類邏輯** | 這題是法規問題？還是標準問題？還是製程問題？ |
| **回答生成邏輯** | 不同類型的題目，組出來的 prompt 不一樣，呈現方式也不一樣 |

這兩件事**可以分開演化**——你可以只改分類方式，不動回答邏輯；也可以只改某一類問題的回答風格，不影響其他類型。

**OOA 的結果：找到了兩個獨立維度。**

### 建議 Prompt（給 AI 的問法）

```
我有一個功能要開發，先幫我做 OOA（需求結構化）。
功能描述：[你的功能說明]
請列出這個功能中，有哪幾個「獨立變化的維度」，
並說明每個維度為什麼可以獨立於其他維度演化。
```

---

## Step 2｜Forces（設計張力）

### 白話說明

Forces 就是「需求之間互相拉扯的力量」。

你找到了幾個獨立維度之後，接下來問：

> **「這些維度如果同時放在同一段程式碼裡，會產生什麼衝突？」**

這是選擇設計模式（Design Pattern）最重要的一步。**Pattern 是用來化解 Forces 的，不是用來炫技的。**

### MedQA 的例子

| Force A | Force B | 衝突點 |
|---|---|---|
| 分類邏輯要貼近醫療器材實務 | 現在用關鍵字 regex 實作 | Regex 無法理解語意，分錯類就全錯 |
| 分類邏輯要能持續改進 | 邏輯寫死在 `worker.js` 裡 | 改分類 = 改部署，成本很高 |
| 不同類型的問題要有不同回答方式 | 目前只有一條 prompt pipeline | 想加新類型，要改核心邏輯 |

你在記憶體裡記錄的「correctNature regex 分類邏輯待改進」，**根本原因就是這裡**：Forces 還沒釐清，就直接用 regex 實作，導致「分類邏輯」和「回答邏輯」糾纏在一起，改一個影響另一個。

**Forces 的結果：發現了三組衝突，特別是「分類維度」和「回答維度」不應該綁在一起。**

### 建議 Prompt（給 AI 的問法）

```
根據以下需求，幫我識別 Forces（設計張力）：
需求維度：[列出你在 OOA 找到的維度]
請說明這些維度如果放在同一個模組／函數裡，
會產生哪些衝突？這些衝突如何互相牽制？
```

---

## Step 3｜OOD（Object-Oriented Design｜物件導向設計）

### 白話說明

有了 Forces，現在才是選 Pattern 的時候。

> **「哪個 Pattern 可以讓這些衝突的力量各自有空間，互不干擾？」**

### 什麼是 Strategy Pattern（策略模式）？

Strategy Pattern 的核心概念：

**把「可以互換的行為」抽出來，讓它們實作同一個介面（Interface），主系統只認介面，不認具體實作。**

用生活比喻：你的手機充電器（主系統）只要有 USB-C 插口（介面），不管你插的是原廠充電器、第三方充電器、還是行動電源（具體實作），都可以充電。換充電器不需要換手機。

#### 順帶認識幾個常見 Pattern（建立全局觀）

Strategy 只是 23 個經典範本之一。先有個概念，遇到對應衝突時才知道有現成解法可查：

| Pattern 名稱 | 解決的 Forces（衝突）| 一句話範本 |
|---|---|---|
| **Strategy（策略）** | 行為要可抽換，但主系統不該被牽動 | 把可變行為封裝成介面（← MedQA 用這個）|
| **Factory（工廠）** | 建立物件的邏輯複雜，散落各處難維護 | 用專門的「工廠」集中負責生產 |
| **Observer（觀察者）** | A 變了，一堆 B 要跟著更新 | 訂閱通知機制（像群組廣播）|
| **Singleton（單例）** | 整個系統只能有一個某物件 | 確保只生產一個實例 |

> 記住核心紀律：**先有 Forces，才去找對應 Pattern。** 不是學了範本到處硬套，而是衝突出現時才認出「這是哪個範本的場景」。

這段是這份筆記最重要的一個真實案例——說明 Forces 怎麼一步一步指向具體的設計決策。

**魚骨圖（Fishbone Diagram / Ishikawa Diagram）** 的六大類別是：

```
人（Man）／機（Machine）／料（Material）／法（Method）／環（Environment）／測量（Measurement）
```

這六個類別**天然就是獨立的分析維度**。把每個類別設計成獨立問答模組，看起來很直覺——但這個決定背後，其實走了一條完整的 Forces 推導：

| 推導步驟 | 內容 |
|---|---|
| **OOA 觀察** | 六個類別可以獨立問答，是天然的分類維度 |
| **Forces 衝突浮現** | 現有 regex 分類邏輯語意不足，遇到醫療器材工程問題容易分錯類 |
| **設計取捨一** | 讓使用者自己選類別（UI 選擇取代自動分類）→ 繞過分類器問題，但把負擔轉給使用者 |
| **Forces 再確認** | 使用者不一定知道問題屬於哪類；跨類別根本原因分析尤其困難 |
| **OOD 決策** | 用 LLM 本身做自動分類，同時對應六個類別設計獨立 Responder |

**結論：魚骨圖六類別 → 獨立問答的需求 → 暴露 Regex 不夠用的 Force → Forces 指向把 `RegexClassifier` 換成 `LLMClassifier`。**

這條推導鏈，就是 Step 2 Forces → Step 3 OOD 之間的真實橋樑。

---

### MedQA 套用 Strategy Pattern 的藍圖

```
MedQASystem（配對系統）
  ├── classifier: NatureClassifier   ← 可抽換的「分類策略」
  └── responder: ResponseStrategy    ← 可抽換的「回答策略」

NatureClassifier（分類器介面 / Interface）
  └── + classify(question): Nature   ← Nature = 人｜機｜料｜法｜環｜測量

  ├── RegexClassifier（現有實作，已知有問題）
  └── LLMClassifier（目標實作）← 用 Claude 本身判斷，語意理解直接升級

ResponseStrategy（回答策略介面 / Interface）
  └── + buildPrompt(question, context): string

  ├── ManResponder（人的問題：人員操作、訓練、疲勞）
  ├── MachineResponder（機的問題：設備校驗、維護、老化）
  ├── MaterialResponder（料的問題：原料規格、供應商、批次）
  ├── MethodResponder（法的問題：SOP、製程參數、驗證）
  ├── EnvironmentResponder（環的問題：溫濕度、潔淨度、存放）
  └── MeasurementResponder（測量的問題：量測系統、MSA、校正）
```

換掉 `LLMClassifier` 之後，兩個 Forces 同時解決：

| Force | 解法 |
|---|---|
| Regex 語意不足、分錯類 | LLM 理解語意，準確率直接升級 |
| 回答邏輯要依類別客製化 | 六個 Responder 各自對應魚骨圖類別，獨立演化 |

### 為什麼選 Strategy Pattern？（理由要說清楚）

| 衝突（Force） | Strategy Pattern 如何化解 |
|---|---|
| 分類邏輯要能改進 | 換 Classifier 不改 MedQASystem，只換插件 |
| 不同類型需要不同回答 | 每個 Responder 獨立，加新類型不改舊程式 |
| 分類與回答不應耦合 | 兩個 Interface 各自獨立，互不知道對方 |

### 關鍵術語補充

**Interface（介面）**：定義「能做什麼」，不定義「怎麼做」。像是一份合約，具體實作類別必須履行合約。

**Open/Closed Principle（開放封閉原則）**：程式碼對擴充開放（可以加新 Classifier），對修改封閉（不需要改 MedQASystem）。這是選 Strategy Pattern 的重要理由之一。

**Dependency Inversion（依賴反轉原則）**：MedQASystem 依賴介面（`NatureClassifier`），不依賴具體實作（`RegexClassifier`）。所以換實作不影響主系統。

**OOD 的結果：畫出了 Class Diagram（類別圖），定義了邊界。**

### 建議 Prompt（給 AI 的問法）

```
根據以下 Forces，幫我選擇適合的 Design Pattern 並說明理由：
Forces：[列出你的衝突清單]
請說明：
1. 你選擇哪個 Pattern？
2. 這個 Pattern 的哪個特性能化解哪個 Force？
3. 畫出對應的 Class Diagram（用文字 ASCII 或 Mermaid 格式）
```

---

## Step 4｜OOP（Object-Oriented Programming｜物件導向程式設計）

### 白話說明

藍圖出來了，現在才讓 AI 填實作細節。

> **「照著 Class Diagram，逐一實作每個類別和介面。」**

這一步是唯一可以完全交給 AI 的步驟，因為邊界已經定義好了——AI 的輸出有框架約束，不會跑出來亂。

### MedQA 的例子：給 AI 的正確 Prompt

❌ **模糊 Prompt（容易失控）**
```
幫我改進 MedQA 的 correctNature 分類邏輯
```

✅ **精確 Prompt（有框架）**
```
請實作 LLMClassifier 類別：
- 它實作 NatureClassifier 介面
- 介面定義：classify(question: string): Nature
- Nature 是 enum，值為：regulatory | standards | process
- 內部呼叫 Claude API 判斷問題性質
- 回傳對應的 Nature enum 值
- 不要修改 MedQASystem 的任何邏輯
```

差別在哪？**你定義了邊界（介面、輸入、輸出、不能動的地方），AI 只負責填實作，不會順手幫你改架構。**

### 建議 Prompt（通用版）

```
請實作以下類別，嚴格按照介面定義：
類別名稱：[ClassName]
實作的介面：[InterfaceName]
介面方法簽名：[method(input: Type): ReturnType]
這個類別的職責：[一句話說明]
不允許修改的部分：[列出不能動的類別或檔案]
```

---

## 整合回顧：MedQA 的完整分析

```
Step 1 OOA    →  發現兩個獨立維度：分類邏輯 / 回答邏輯
                 魚骨圖六類別（人機料法環測量）= 天然的回答維度

Step 2 Forces →  衝突一：Regex 語意不足，分錯類就全錯
                 衝突二：分類與回答邏輯耦合，改一個影響另一個
                 衝突三：六類別各需不同 prompt，單一 pipeline 無法勝任
                 → 魚骨圖獨立問答的需求，暴露了 RegexClassifier 的根本缺陷

Step 3 OOD    →  選 Strategy Pattern
                 NatureClassifier：RegexClassifier → 換成 LLMClassifier
                 ResponseStrategy：六個 Responder 對應魚骨圖六類別

Step 4 OOP    →  先實作 LLMClassifier（classify 回傳 人｜機｜料｜法｜環｜測量）
                 再逐一實作六個 CategoryResponder
                 MedQASystem 本體完全不動
```

---

## 關鍵術語中英文對照表

| 中文 | 英文 | 一句話說明 |
|---|---|---|
| 物件導向分析 | OOA (Object-Oriented Analysis) | 需求結構化，找出獨立變化的維度 |
| 物件導向設計 | OOD (Object-Oriented Design) | 有了 Forces 後，套對應 Pattern |
| 物件導向程式設計 | OOP (Object-Oriented Programming) | 照藍圖實作，讓 AI 填細節 |
| 設計張力 | Forces | 需求之間互相牽制的衝突力量 |
| 策略模式 | Strategy Pattern | 把可互換行為封裝成介面，讓主系統只認介面 |
| 介面 | Interface | 定義「能做什麼」的合約，不定義「怎麼做」 |
| 類別圖 | Class Diagram | 實作前的藍圖，定義類別邊界與關係 |
| 開放封閉原則 | Open/Closed Principle | 對擴充開放，對修改封閉 |
| 依賴反轉原則 | Dependency Inversion Principle | 高層模組依賴介面，不依賴具體實作 |
| 設計模式 | Design Pattern | 前人驗證過的「解題範本」，針對常見設計問題的標準解法 |
| 耦合 | Coupling | 兩個模組互相依賴的程度（越低越好）| 
| 抽換 | Swap / Plug-in | 換掉一個實作，不影響其他部分 |
| 分類器 | Classifier | 負責判斷輸入屬於哪個類別的模組（MedQA：NatureClassifier）|
| 回答策略 | Response Strategy | 針對不同類別生成對應回答的模組（MedQA：六個 CategoryResponder）|
| 魚骨圖 | Fishbone Diagram / Ishikawa Diagram | 根本原因分析工具，六大類別：人機料法環測量 |
| 設計取捨 | Trade-off | 在兩個衝突的 Force 之間做出選擇，沒有完美解 |

---

## 進階篇｜框架學會之後，遇到真實混亂怎麼辦？

> 投影片的案例（MatchingSystem）是乾淨的：兩個策略彼此獨立、沒有層次、沒有關聯。
> 但真實問題（MedQA 魚骨圖）有六個類別、有主因子→子因子的層次、還有跨因子的關聯。
> 這一篇談的，是**把框架套到髒案例後，自己推導出來的設計決策**。

### 核心觀念：Forces 是遞迴的

投影片把流程畫成一條直線（OOA → Forces → OOD → OOP），容易讓人以為「分析一次就結束」。但真實設計不是這樣：

> **每往下深入一層，新的 Forces 就會重新浮現。**

```
第一層 Forces：分類邏輯 vs 回答邏輯 → 用 Strategy Pattern 解
     ↓ 深入到「回答邏輯」內部
第二層 Forces：子因子要不要交給 AI？→ 結構固定 vs 內容生成的取捨
     ↓ 深入到「因子之間」
第三層 Forces：主因子有關聯怎麼辦？→ 獨立分析 vs 關聯合成的取捨
```

**遇到設計卡關時，先問：我是不是進到了新的一層，卻還在用上一層的答案？**

---

### 難題一｜子因子判斷交給 AI，品質怎麼保？

當一個主因子（例如「機」）內部還要再判斷子因子時，全部寫死會交代不完，全部交給 AI 又怕品質跑掉。

**這裡的 Forces：**

| Force A | Force B |
|---|---|
| 子因子太多，寫死交代不完 | 品質要穩定、可比較 |

**化解原則：結構你管，內容 AI 管**

```
你定義（固定）：機 → 子因子清單［設備校驗、維護週期、老化程度、參數設定］
AI 負責（生成）：判斷哪個子因子最相關，並展開分析
```

這樣做擋住了兩個品質風險：

| 風險 | 英文 | 固定結構如何擋住 |
|---|---|---|
| 幻覺 | Hallucination | AI 不能自己發明子因子，只能從清單選 |
| 粒度不一致 | Inconsistent Granularity | 子因子層次固定，每次分析可比較 |

**建議 Prompt：**

```
你是[機器設備]分析專家。
以下是可能的子因子（只能從這份清單選，不要自行新增）：
[設備校驗、維護週期、老化程度、參數設定]
根據使用者問題，判斷最相關的子因子，並針對每個相關子因子展開根本原因分析。
問題：{question}
```

關鍵就是那句「**只能從這份清單選，不要自行新增**」——這一句話就是你的品質防線。

---

### 難題二｜主因子之間的關聯性怎麼判斷？

魚骨圖原始設計假設六個主因子是平行、互不相交的。但真實問題常常跨因子：

> 「操作員（人）沒按 SOP（法）操作，導致設備（機）參數漂移。」
> 這是 人 × 法 × 機 三個因子的交叉。

這個難題沒有完美解，只有**分階段的取捨**。以下是三個選項，建議照順序演進：

| 版本 | 做法 | 優點 | 缺點 |
|---|---|---|---|
| **v1｜獨立分析** | 六因子各自分析，使用者自行綜合 | 簡單、品質穩定 | 跨因子根本原因被切碎 |
| **v2｜關聯合成** | 六因子分析完，加第七個 prompt 找關聯與根本原因鏈 | 保留獨立分析品質，再做合成 | token 消耗高 |
| **v3｜使用者標記** | 讓使用者點選「這兩個因子有關聯」，再深入分析 | 領域知識介入，品質最高 | 多一個 UI 步驟 |

**v2 的合成 Prompt 範例：**

```
以下是針對[人/機/料/法/環/測量]六個因子的獨立分析結果：
{六個因子的分析輸出}
請找出這些因子之間的關聯，並推導出最可能的「根本原因鏈」
（例如：A 因子 → 導致 B 因子 → 最終造成問題）。
```

---

### 這一篇真正的方法論價值

比起記住具體答案，更值得帶走的是這套**通用的思考姿態**：

1. **遞迴套用 Forces**——卡關時，先確認自己在哪一層，是不是該重新做一次 Forces 分析
2. **結構固定／內容生成的分工**——凡是「怕 AI 品質跑掉」的場景，都用這招：你控結構，AI 填內容
3. **分階段落地（v1→v2→v3）**——不追求一次完美，先求穩定可控，每加一層複雜度都要對應一個被新需求觸發的 Force

> **一句話總結：框架帶你走到設計的門口，門裡面的每一層，都要再做一次 Forces。**

---

### 進階篇術語補充

| 中文 | 英文 | 一句話說明 |
|---|---|---|
| 幻覺 | Hallucination | AI 編出聽起來合理但不存在的內容 |
| 粒度 | Granularity | 分析的細緻程度；粒度不一致會無法比較 |
| 根本原因鏈 | Root Cause Chain | A→B→C 的因果推導，找出問題的源頭 |
| 遞迴 | Recursion | 同一套方法在更深一層重複套用 |
| 分階段落地 | Phased Rollout | 不一次做完，分 v1/v2/v3 逐步演進 |

---

## 實戰篇｜MedQA 目前建議改善的思路對策總表

> 前面的框架篇與進階篇談的是「方法」。這一篇把方法收斂成**可執行的改善清單**——
> 是這份筆記討論到目前為止，針對 MedQA 的所有具體對策的整合。

### 對策一覽表

| # | 問題 | 改善對策 | 對應框架 | 階段 |
|---|---|---|---|---|
| 1 | correctNature regex 語意不足、分錯類 | 把 `RegexClassifier` 換成 `LLMClassifier`，用 Claude 判斷類別 | Strategy Pattern｜抽換策略 | v1 必做 |
| 2 | 分類邏輯與回答邏輯耦合 | 拆成兩個介面（`NatureClassifier` / `ResponseStrategy`），各自獨立演化 | OOD｜解耦 | v1 必做 |
| 3 | 單一 pipeline 無法應付多類別 | 對應魚骨圖六類別，建六個 `CategoryResponder` | Strategy Pattern｜回答策略 | v1 必做 |
| 4 | 子因子太多寫死交代不完、但怕 AI 品質跑掉 | 結構固定（子因子清單寫死）＋ 內容生成（AI 填）＋「不要自行新增」防線 | 安全包絡 Safety Envelope | v1 必做 |
| 5 | 主因子之間有跨類別關聯（人×法×機）| 先 v1 獨立分析 → v2 加合成步驟 → v3 使用者標記 | 分階段落地 | v2/v3 演進 |
| 6 | 分類信心高低不同，固定流程不夠彈性 | 滑動自主：信心高→AI 全自動；信心低→轉「請使用者確認」 | 滑動自主 Sliding Autonomy | v3 進階 |

---

### 落地優先順序（建議施工順序）

```
第一波（v1）：把架構打對
  ① 寫 LLMClassifier，replace 掉 RegexClassifier
  ② 確認 NatureClassifier / ResponseStrategy 兩個介面分離
  ③ 建六個 CategoryResponder，子因子清單寫死在各 Responder 內
  ④ 每個 Responder 的 prompt 加「只能從清單選，不要自行新增」
  → 此時 MedQASystem 本體完全不動（Open/Closed 發揮作用）

第二波（v2）：補上跨因子關聯
  ⑤ 六因子分析完，加第七個合成 prompt，找根本原因鏈

第三波（v3）：讓系統會自己調整
  ⑥ 引入信心分數，高信心全自動、低信心轉人工確認（滑動自主）
```

---

### 為什麼這個順序？（用框架解釋）

第一波先做,是因為它把**結構性的 Force**全部解決——分類錯誤、邏輯耦合、品質失控,這三個是「不做就會持續惡化」的根本問題。而且因為有 Strategy Pattern 的解耦,**第一波做完不會擋住第二、三波**,可以安全地逐步加。

第二、三波是**體驗性的 Force**——沒有它系統也能用,只是不夠聰明。把它們延後,符合「先求穩定可控,再加複雜度」的原則。

> **核心判準:先解決「不做會爛掉」的 Force,再處理「做了會更好」的 Force。**

---

### 控制策略的跨界對照（為什麼這套思路通用）

這套「結構/內容分工 + 分階段自主」的思路,和機器人控制領域高度同構,可作為設計信心的佐證:

| MedQA 對策 | 機器人控制對應概念 | 共同原則 |
|---|---|---|
| 結構你管、內容 AI 管 | 分層控制 Hierarchical Control | 高層人控、低層自主 |
| 固定清單禁止新增 | 安全包絡 Safety Envelope | 自主但有硬邊界 |
| v3 使用者標記關聯 | 共享控制 Shared Control | 不確定時交還控制權 |
| v1→v2→v3 分階段 | 監督式自主 Supervisory Control | 人設邊界、機器執行 |
| 信心高低調整自動化程度 | 滑動自主 Sliding Autonomy | 自主程度隨情境即時調整 |

> 註：機器人控制概念為該領域公認術語，此處作為跨界類比，說明「人機分工」的設計哲學是通用的，並非特定廠商的內部做法。

---

### 實戰篇術語補充

| 中文 | 英文 | 一句話說明 |
|---|---|---|
| 安全包絡 | Safety Envelope | 允許自主，但用硬邊界擋住失控 |
| 分層控制 | Hierarchical Control | 高層管目標、低層管即時執行 |
| 共享控制 | Shared Control | 人與系統同時控制，動態仲裁權重 |
| 監督式自主 | Supervisory Control | 人設邊界與目標，系統自主執行 |
| 滑動自主 | Sliding Autonomy | 自主程度不固定，隨情境/信心即時調整 |

---

## 控制篇｜Prompt 控制手法：錨定 + 限制，以及更多

> 前面一直反覆出現一句「只能從清單選、不要自行新增」——它其實是一個更大體系的縮影。
> 這一篇把「人 ↔ AI 的溝通介面」拆開，說明控制 AI 輸出品質的完整手法。
> 對照來源：個人法規 AI 指南（regulatory_ai_guide）PART 1「降低幻覺與法規定錨」。

### 核心：Prompt 是「半結構化介面」

```
機械語言（Regex / 程式碼）              自然語言（人類對話）
└─ 硬規則 syntax：差一字就失敗   ←→   └─ 軟規則：模糊也能溝通

                      Prompt 在中間
                          ↑
            軟規則：不遵守不報錯，但品質會差
```

Prompt 用自然語言（像跟人說話），卻要求機械語言的嚴謹（邊界、格式、約束都要明確）。它的目標不是「簡單到小學生懂」，而是「**完整到陌生專家不用反問你就能做對**」——前者要求淺，後者要求的是**沒有模糊空間**。

---

### 兩個基礎手法：錨定 + 限制

這是所有控制手法的地基，貫穿這份筆記與法規指南。

#### 錨定（Anchoring）

LLM 是機率生成，會往「機率最大」的方向跑。**錨定就是用精確指定，把 AI 拉到你要的位置。**

| 場景 | 錨定做法 |
|---|---|
| 法規寫作 | 指定版本：`Use ISO 14971:2019 (NOT 2007)` |
| MedQA 分類 | 指定 enum：回傳值只能是 人/機/料/法/環/測量 |
| 一般任務 | 指定角色：「你是有 20 年經驗的 XX 專家」 |

> 小技巧：AI 看得懂 Markdown 強調符號。**大寫 = keep attention，粗體 = 重點**，會讓 AI 對該處給更多機率權重。所以 `**NOT 2007**` 比 `not 2007` 有效。

#### 限制 / 負面排除（Negative Constraints）

**明講「不允許什麼」，劃出禁區。** 這正是「不要自行新增子因子」那句話的本質。

| 場景 | 限制做法 |
|---|---|
| 法規寫作 | 「不允許用 ISO 14971 的 ALARP，要用 MDR 的 AFAP」 |
| MedQA 子因子 | 「只能從這份清單選，不要自行新增」 |
| 防幻覺 | 「找不到來源就標 `[UNSUPPORTED]`，不要硬掰」 |

> **錨定告訴 AI「往這裡走」，限制告訴 AI「別往那裡走」。兩者一正一反，夾出正確的輸出空間。**

---

### 進階手法：錨定 + 限制之外還有四層

控制 AI 品質不只兩招。完整的手法至少六層，後四層是針對「高風險、不能出錯」場景的加固：

| # | 手法 | 英文 | 白話 | MedQA 可借用之處 |
|---|---|---|---|---|
| 1 | 錨定 | Anchoring | 指定版本/角色/格式，拉到正確位置 | 分類 enum、專家角色 |
| 2 | 限制 | Negative Constraints | 明講禁區，劃出邊界 | 子因子清單禁止新增 |
| 3 | 思維鏈強制 | Step 0 Reasoning | 先寫推論再產出，避免跳步 | 根本原因鏈分析先說理由 |
| 4 | 自我批判 | Reflexion | 叫 AI 扮稽核員回頭批改自己 | 分類完反問「會不會跨類別」 |
| 5 | 來源分級＋標註 | Source Tiers + Claim Flagging | 每個聲明標來源等級與信心 | 信心標註對應滑動自主 |
| 6 | 量化與追溯 | Quantification + Traceability | 禁止憑空數字，要可回溯 | 分析結論要對得回原始問題 |

---

### 信心標註 ↔ 滑動自主：一個漂亮的呼應

法規指南用 **Claim 標註旗標**讓 AI 對自己的把握度做分級：

| Flag | 意思 | 能否直接用 |
|---|---|---|
| `FACT` | 有明確來源支持 | 可作草稿依據 |
| `INFERENCE` | 基於證據的專業解讀 | 需人確認 |
| `TBD` | 資訊不足 | 不得納入正式結論 |

這跟你 MedQA 的「滑動自主」是同一個靈魂：

> **不是讓 AI 假裝什麼都確定，而是讓它誠實標示哪裡有把握、哪裡沒有，人類再針對沒把握的地方介入。**

`FACT/INFERENCE/TBD` 是「對內容的信心標註」，「分類信心高→全自動／低→請使用者確認」是「對判斷的信心標註」——**本質都是讓 AI 暴露自己的不確定性，把人類注意力導向最該查核的地方。**

---

### 三份筆記的體系關係

這份筆記討論到現在，其實串起了三個抽象層級：

```
設計思維筆記（本檔）：錨定 + 限制是「通用設計原則」
        ↓ 具體化到法規場景
法規 AI 指南 PART 1：錨定 + 限制的「法規場景招式」（版本定錨、負面排除）
        ↓ 具體化到分類器設計
MedQA 重構：錨定 + 限制的「分類器招式」（enum 錨定、子因子禁止新增）
```

**同一個哲學，三個抽象層級。** 看懂這個，下次遇到任何「怕 AI 輸出失控」的場景，你都知道要從「錨定（往哪走）＋限制（別往哪走）」開始，再視風險高低加上後面四層。

---

### 控制篇術語補充

| 中文 | 英文 | 一句話說明 |
|---|---|---|
| 錨定 | Anchoring | 用精確指定把 AI 拉到正確位置 |
| 負面排除 | Negative Constraints | 明講禁區，劃出不允許的範圍 |
| 思維鏈強制 | Step 0 / Chain-of-Thought | 先寫推論再產出，避免跳步 |
| 自我批判 | Reflexion | 讓 AI 扮稽核員回頭批改自己的草稿 |
| 來源分級 | Source Tiers | 把來源分官方/認證/業界三層 |
| 聲明標註 | Claim Flagging | 每個聲明標來源等級與信心狀態 |
| 半結構化介面 | Semi-structured Interface | 用自然語言，但要求機械語言的嚴謹 |

---

## 實戰篇 II｜用框架解剖真實系統：MedQA worker.js 檢討

> 前面的實戰篇是「該怎麼改」的對策清單。這一篇更進一步——
> 把整套框架當成手術刀，**實際解剖 MedQA 現有的 worker.js（v5.3-b06）與 index.html**，
> 找出架構層級的系統性弱點，並轉成「給 AI / Codex 的可執行施工單」。

### 目前的實際流程（七階段）

```
① 器材識別     identify_device    (haiku)   ← 結合 FDA 510k + MAUDE
② AI 追問       stream_navigate    (haiku)   ← DMAIC 的 Define 階段
③ 整理因子     summarize_factors  (sonnet)
④ 管道分類     verification_plan  (sonnet)  ← 7 分支決策樹
⑤ 魚骨圖 6M    fishbone_generate  (opus)
⑥ ISO 缺口     iso_crosscheck     (haiku)   ← 對照 iso_map.json
⑦ 報告產出     plan_narrative     → 列印 PDF
```

---

### 三個系統性弱點（對照本筆記的框架）

#### 弱點一：分類邏輯散落在七個 prompt 裡（最根本）

本筆記反覆強調「分類邏輯與回答邏輯要解耦」，但 worker.js **完全沒有落實**。每個 mode 的 system prompt 裡都各自塞了一大段判斷規則：

| mode | 內嵌的判斷規則 | 行數 |
|---|---|---|
| `verification_plan` | 7 分支 pipeline 決策樹 | 505–511 |
| `iso_crosscheck` | 4 段 ISO 防呆「特別注意」 | 624–634 |
| `fishbone_generate` | 材料常識＋臨床時序判讀 | 1318–1328 |

這正是本筆記的反面教材：`NatureClassifier`（分類）和 `ResponseStrategy`（回答）糾纏在同一個 prompt 裡。改一條 ISO 防呆，要動整個 `iso_crosscheck` 的 prompt，無法單獨測試。

#### 弱點二：規則用「自然語言堆疊」維護，不是結構化資料

違反控制篇的核心「**結構你管、內容 AI 管**」。現況是「結構也丟給 AI 用散文描述」——例如 ISO 11607 vs 10555 的區分（line 628–630）是三行中文叮嚀「絕不是 11607」。每加一個案例就要再寫一段散文，正是「子因子交代不完」的翻版。

#### 弱點三：opus 用在魚骨圖，算力放錯地方

魚骨圖用 opus（最貴）、分類判斷用 haiku/sonnet。但按框架，真正需要強推理的是**分類決策**（7 分支、ISO 對應），魚骨圖反而發散、容錯高。算力與 Force 性質錯配。

---

### 改善策略（依「先修不做會爛掉的」排序）

| 波次 | 改善 | 對應框架原則 |
|---|---|---|
| v1 | 把 `verification_plan` 決策樹、`iso_crosscheck` 標準對應，從 prompt 散文抽成結構化 JSON 規則表 | 結構你管、內容 AI 管 |
| v1 | 分類邏輯集中成獨立 `classify` 步驟，各 responder 只負責生成 | NatureClassifier / ResponseStrategy 解耦 |
| v2 | ISO 防呆改成「對照表＋禁區清單」，新增案例只改資料不改 prompt | 錨定＋限制（負面排除結構化）|
| v2 | 重評模型分流：sonnet/opus 給分類決策，魚骨圖降 haiku/sonnet | Forces 性質決定資源分配 |
| v3 | 擴充 `risk` 欄位為信心分數，低信心轉人工確認 | 滑動自主 |

---

## 給 AI / Codex 的執行策略（施工單）

> 以下是「等我有時間、AI/Codex 有 quota 時要執行」的具體指令。
> 每張工單都遵循本筆記原則：先給藍圖與邊界，再讓 AI 填實作。
> **施工鐵則：每張工單獨立可測、可回滾；改完跑一次完整七階段流程驗證無回歸再進下一張。**

### 工單 WO-1（v1）｜抽出 verification_plan 決策樹為 JSON 規則表

```
【背景】worker.js 的 verification_plan mode（約 line 490–577）把 7 分支
pipeline 決策樹寫死在 system prompt 散文裡，無法單獨維護或測試。

【目標】把決策樹抽成結構化 JSON 規則表，worker 讀表後只讓 AI 做
「因子 → pipeline」的對應填空，不再讓 AI 自己記憶整棵樹。

【邊界 — 嚴格遵守】
- 只重構 verification_plan，不動其他 mode。
- 7 個 pipeline 的 enum 值不變：experiment / compliance / method_dev /
  risk_evidence / use_side / supplier / uncontrollable。
- tool 的 input_schema 不變（plans / summary 結構保持相容）。
- 既有的 link 因果路徑要求、subtype 準則保留。

【實作步驟】
1. 在 worker.js 頂部新增 const PIPELINE_RULES = [...]，把 line 505–511
   的 7 分支「判斷順序（命中即停）」轉成陣列，每筆含：
   { order, pipeline, trigger_keywords, subtype_options, example }。
2. system prompt 改為：引用 PIPELINE_RULES，要求 AI「依序比對，命中即停」，
   並加負面排除「不在表內的 pipeline 一律不得使用」。
3. 不要改變對外輸出格式。

【驗收】用 3 個既有因子（1 個 experiment、1 個 compliance、1 個
uncontrollable）跑驗證，pipeline 分類結果與重構前一致。
```

### 工單 WO-2（v1）｜分類與回答解耦

```
【背景】分類邏輯散落在 navigate / verification_plan / iso_crosscheck /
fishbone_generate 各自的 prompt 裡，違反 Strategy Pattern 解耦原則。

【目標】建立一個獨立的「分類層」，把「這個因子/問題屬於哪一類」的判斷
集中，各生成 mode 只負責「依已定類別生成內容」。

【邊界】
- 這是「分層」重構，不是「改演算法」，分類結果的語意要與現況等價。
- 先做 verification_plan 這一條鏈當試點，成功再推廣。
- 不可破壞既有 mode 的對外 JSON 介面。

【實作步驟】
1. 新增 classify mode（或內部函式），輸入因子，輸出結構化 category。
2. verification_plan 改為「接收已分類的 category」，prompt 只剩生成邏輯。
3. 保留舊路徑為 fallback，加 feature flag 可切換，方便 A/B 比對。

【驗收】同一批因子，新舊路徑分類一致率 ≥ 95%；不一致的案例逐一檢視。
```

### 工單 WO-3（v2）｜ISO 防呆改結構化對照表

```
【背景】iso_crosscheck（line 580–636）的 ISO 標準區分（11607 vs 10555、
10993 適用範圍、13485 不配個別因子）是散文叮嚀，每加案例就再寫一段。

【目標】把「標準 → 適用情境 / 禁區」改成 JSON 對照表，新增案例只改資料。

【邊界 — 防幻覺最重要】
- 維持現有鐵則：clause 只能引用「已建檔章節」，嚴禁 AI 自行發明 Annex 號。
- 對照表是「限制用」，不是放寬；負面排除（哪些情境「絕不是」某標準）要保留。

【實作步驟】
1. 新增 ISO_DISAMBIGUATION = [
     { standard, applies_when:[...], never_when:[...], note }
   ]，把 line 624–634 的四段散文轉成資料。
2. prompt 改為引用此表，要求「先比對 never_when，命中則排除該標準」。
3. 11607（無菌包裝封口）vs 10555（導管本體接合）的區分務必入表。

【驗收】用「導管 Hub 熱熔接合漏水」測試，結果必須對到 10555-1、
絕不可出現 11607。
```

### 工單 WO-4（v2）｜模型分流重新校準

```
【背景】目前 opus 用在 fishbone_generate（發散、容錯高），分類決策卻用
haiku/sonnet（需強推理）。算力與任務難度錯配。

【目標】依「Force 性質」重新分配模型：強推理的分類決策給高階模型，
發散的生成任務給中階模型。

【邊界】
- 只改 MODE_MODEL / TOOL_MODEL_POLICY 的對應，不動邏輯。
- 改完要實測成本與品質，不是純拍腦袋；保留改前數據對比。

【實作步驟】
1. verification_plan、iso_crosscheck（分類決策）→ 升 sonnet（必要時 opus）。
2. fishbone_generate → 試降 sonnet，比對魚骨圖品質是否可接受。
3. 記錄每個 mode 改前/改後的 token 成本與主觀品質分數。

【驗收】總成本不上升的前提下，分類錯誤率下降；魚骨圖品質無明顯退步。
```

### 工單 WO-5（v3）｜分類信心標註 → 滑動自主

```
【背景】verification_plan 已有 risk 欄位（high/medium/low），但沒有
「分類本身的信心」。低信心時系統仍直接輸出，使用者無從得知該不該複核。

【目標】為分類結果加 confidence 欄位，低信心自動轉「請使用者確認」，
實作滑動自主（高信心全自動、低信心交還人工）。

【邊界】
- 沿用法規 AI 指南的 FACT/INFERENCE/TBD 精神：誠實標示不確定。
- 不是讓 AI 假裝確定，而是把人類注意力導向最該複核的因子。

【實作步驟】
1. tool input_schema 的 plans 項新增 confidence: { enum:['high','low'] }。
2. 前端對 confidence=low 的因子加視覺標記，並提供「確認 / 改判」按鈕。
3. confidence 判準寫進 prompt：證據明確→high；需推測→low。

【驗收】刻意餵一個模稜兩可的因子，confidence 應標 low 並觸發前端複核 UI。
```

---

### 施工總原則（給未來的自己與 AI）

1. **一次一張工單**——WO-1 → WO-2 → … 依序，每張獨立驗收，不要一次全改。
2. **先 v1 再 v2 再 v3**——架構打對（解耦、結構化）才疊體驗優化（分流、滑動自主）。
3. **每張工單都有明確邊界與驗收**——這就是「錨定＋限制」：告訴 AI 往哪走、別動什麼、怎麼算成功。
4. **保留 fallback 與對比數據**——可回滾，能用數字證明改善，不靠感覺。
5. **Codex 協作時先驗 SHA**——推送前確認遠端版本，避免覆蓋平行修改（沿用 LitDB 既有紀律）。

---

## 模組篇｜架構獨立、節點 check、外行人地圖

> 這一篇回答三個實際訴求：(1) 好用的模組要能被別的程式直接引用；
> (2) AI 流程每個節點都要 check 品質，不一路往下衝；(3) 外行人打開
> 程式碼，光看標註就知道「這段是哪個階段」。三者其實是同一件事的三面。

### 一、模組化：能被引用的模組長什麼樣

你觀察到「510k 搜尋結果很好，未來其他程式可以直接擷取引用」——這在工程上叫 **可重用模組（Reusable Module / Component）**，能被安全引用的關鍵，就是本筆記已學過的 **介面（Interface）**。一個模組要能被別人引用，必須同時滿足四條：

| 條件 | 英文 | 白話 | 510k 模組的例子 |
|---|---|---|---|
| 單一職責 | Single Responsibility | 只做一件事，做好 | 只負責「查 510k 並回傳結構化結果」 |
| 明確輸入輸出 | Defined I/O Contract | 進什麼、出什麼，固定 | 進：產品碼/器材名 → 出：固定 JSON |
| 不依賴外部狀態 | No Side Effects | 不偷用全域變數 | 不依賴 `window._deviceInfo` |
| 可獨立測試 | Independently Testable | 單獨拿出來能跑 | 給一個產品碼就能驗證 |

> **這就是「架構獨立」的技術定義。** 反過來，一個模組若偷用全域變數、輸出格式不固定，別人就不敢引用——一引用就會牽動到它的隱藏依賴。

### 二、雙層 check：能用程式驗的，絕不花 AI

「在各節點 check、check、check」是正確的 AI 工程直覺，但 check 要分兩層，順序不能反：

```
AI 輸出
   ↓
① 結構 check（硬驗證，程式做，不花 token）
   欄位在不在？型別對不對？enum 合法嗎？陣列空不空？
   ↓ 通過
② 語意 check（軟驗證，必要時才用 AI，花 token）
   這個分類合理嗎？有沒有幻覺？跨類別了嗎？
   ↓ 通過
下一個節點
```

**核心原則：能用程式 check 的，絕不花 AI。** worker.js 其實已有①的雛形（如 `verification_plan` 檢查 `Array.isArray(result.plans)`），但散落各處、不成系統，值得收斂成一個統一的「驗證層」模組。

### 三、階段標註：給外行人的地圖

目標是打開 worker.js / index.html，**不讀懂程式、光看註解就知道「這段是第幾階段、做什麼」**。工程上叫 **區段標記（Section Marker）**。建議的標註規範：

```javascript
// ╔══════════════════════════════════════════════╗
// ║ 【階段 ④】管道分類  mode: verification_plan     ║
// ║ 輸入：因子清單 + 器材資訊                         ║
// ║ 輸出：每個因子的 pipeline 分類                    ║
// ║ 可獨立引用：是（不依賴全域狀態）                   ║
// ║ check 點：plans 為陣列、pipeline 在 7 種 enum 內  ║
// ╚══════════════════════════════════════════════╝
```

滑到任何一段，像看路標一樣知道自己在流程哪裡。每個區段固定五個欄位：階段編號、輸入、輸出、可否獨立引用、check 點。

---

### ⚠️ 順帶提出的不合理處與改善建議（誠實版）

讀完 worker.js 後，有幾個影響「可用性」的點，本於對你有幫助的立場直說：

**1. worker.js 是一個「巨石檔」（Monolith），混了四套不相干的系統**

這支 1660 行的檔案同時處理：MedQA 的分析 modes、LitDB 報告儲存（/reports）、文獻寫入（/papers）、Google Patents 圖片爬取（/patent-images）。這四件事彼此無關，卻擠在同一個檔案、共用同一個 Worker。

> 問題：改 MedQA 可能誤傷 LitDB；任何一段出錯整個 Worker 受影響；新人（或外行的你）打開檔案無從下手。
> 建議：長期應拆成獨立 Worker（medqa-worker / litdb-worker 已部分分離，但程式碼仍混在一起）。短期至少用「階段標註」把四大區塊在檔案內切清楚——這也是 WO-7 先做標註的價值：**先讓邊界可見，才談得上拆分。**

**2. 全域狀態（`window._deviceInfo`、`window._maudeProblems`…）讓模組無法獨立**

前端大量用 `window._xxx` 傳遞狀態，這正是「不依賴外部狀態」的反例。510k 模組之所以好引用，正因為它沒這毛病；其他模組要跟進。

**3. 510k 內容萃取依賴 Claude API 讀 PDF，成本高且脆弱**

這是已知的 recall gap 來源。建議中長期把「能用純程式解析的欄位」（產品碼、申請人、日期）和「需要 AI 理解的欄位」分開——又是一次雙層 check 的應用：結構化欄位用程式抓，只有真正需要語意的才花 AI。

**4. 沒有統一的錯誤回傳格式**

各 mode 的錯誤有的回 `{error}`、有的回 `{result:null, error}`、有的帶 `_debug`。前端要各別處理，容易漏。建議統一成一個 `{ok, data, error, stage}` 信封格式——可用性立刻提升。

---

## 新增施工單（模組化與可用性）

### 工單 WO-6（v2）｜把 510k 搜尋抽成獨立可引用模組（示範樣板）

```
【背景】510k 搜尋是目前品質最好的功能，但邏輯與其他程式碼混在一起，
其他工具（如未來的 Regulatory Roadmap）無法直接引用。

【目標】把 510k 搜尋抽成一個獨立模組，滿足「可重用模組四條件」，
當作未來所有模組的示範樣板。

【邊界 — 模組四條件，逐條驗收】
1. 單一職責：模組只做「查 510k → 回傳結構化結果」，不夾帶 UI 或全域狀態。
2. 明確 I/O：輸入只接受參數物件 { productCodes, deviceName }；
   輸出固定 JSON schema（先定義好欄位再實作）。
3. 不依賴外部狀態：嚴禁讀寫 window._xxx，所有輸入走參數。
4. 可獨立測試：附一個最小測試，給一個產品碼能單獨跑出結果。

【實作步驟】
1. 先寫出輸出 JSON schema（這是介面契約，最重要，先定再寫）。
2. 把現有 510k 邏輯搬進模組，切斷所有全域依賴，改走參數。
3. 寫一個 example 呼叫，證明別的程式能直接 import 使用。

【驗收】用一個全新的最小 HTML，只 import 這個模組就能查 510k，
不需要 MedQA 的其他任何程式碼。
```

### 工單 WO-7（v1，最先做）｜全檔加上「階段標記註解」（外行人地圖）

```
【背景】worker.js 是 1660 行巨石檔，混了 MedQA / LitDB reports / papers /
patent-images 四套系統，外行人打開無從定位。

【目標】不改任何邏輯，只加註解，讓非工程背景者光看標記就知道
「這段屬於哪個系統、哪個階段、輸入輸出是什麼」。

【邊界 — 絕對不動邏輯】
- 只加註解，一行程式碼都不改。這張工單零風險，應最先做。
- 標記格式統一用本筆記「模組篇」的五欄位框（階段/輸入/輸出/可否獨立引用/check點）。

【實作步驟】
1. 先在檔案最頂端加「總目錄」註解，列出四大系統各自的行數範圍。
2. 每個 mode / 端點開頭，加五欄位標記框。
3. 四大系統之間用粗分隔線註解切開（MedQA ═══ / LitDB ═══ …）。

【驗收】你（外行人）能只靠捲動＋讀註解，正確說出任一段程式屬於哪個
系統、哪個階段——不需要讀懂任何一行 JavaScript。
```

> 施工順序微調建議：**WO-7 應排在所有工單最前面**（零風險、純加註解），因為「先讓結構可見」是後續所有重構（解耦、拆分、模組化）的前提。地圖畫好了，才知道路要怎麼修。

---

### 模組篇術語補充

| 中文 | 英文 | 一句話說明 |
|---|---|---|
| 可重用模組 | Reusable Module | 滿足四條件、能被別的程式直接引用的程式單元 |
| 單一職責 | Single Responsibility | 一個模組只負責一件事 |
| 輸入輸出契約 | I/O Contract | 固定的進出格式，引用者照契約用 |
| 副作用 | Side Effect | 偷改全域狀態等隱藏依賴（要避免）|
| 驗證層 | Validation Layer | 集中做結構 check 的模組 |
| 巨石檔 | Monolith | 把多套不相干系統塞在一起的大檔案（反模式）|
| 區段標記 | Section Marker | 讓人快速定位程式段落的標註 |
| 錯誤信封 | Error Envelope | 統一的回傳格式（ok/data/error/stage）|

---

## DMAIC2 設計篇｜全流程規格與 AI 強化策略

> 這一篇是 DMAIC2（新版 CAPA 分析工具）的完整設計文件。
> 對應實作檔案：`dmaic2.html`（模擬版）、`dmaic2_data.json`（獨立 schema）。
> 流程確認後再轉成 Cloudflare Worker API 版本。

---

### 設計原則（開發前 checklist）

- `dmaic2_data.json` 獨立，不與 MedQA / LitDB 任何系統共用
- 模組一（醫療器材檢索）：邏輯不動，只切模組邊界與標註
- 先在 `dmaic2.html` 模擬，試驗 OK 後才轉 API
- 六因子：使用者填寫主導，AI 整理補漏，ISO 13485 CAPA 可追溯
- DOE 正式移除；ISO 資料庫已移除，改由 AI 知識建議（輸出標註「請 RA 確認版本」）
- 不含 DOE 的驗證工具：SPC / MSA / IFU / 供應商調查 / 量測方法開發 / 風險文件

---

### 三大模組架構

#### 模組一｜醫療器材檢索模組（Medical Device Search Module）

```
器材識別（identify_device，邏輯不動）
    ↓
FDA API 檢索 → 510k PDF × 3 篇
    ↓
驗證項萃取（結構 check → 入 dmaic2_data.json）
```

對外輸出：`verification_items`（510k 萃取，帶入模組三對照用）

---

#### 模組二｜CAPA 核心模組（CAPA Core Module）

**使用者填寫層（人主導，ISO 13485 CAPA 可追溯）**

```
六因子表單：人 / 機 / 料 / 法 / 環 / 測量
每個因子各填初步描述（自由文字）
紀錄以人填為主 → 稽核決策可追溯
```

**AI 協助層（強化版 v2）**

```
語意整理：口語 → 結構化
    +
即時追問（強化 A）：填一個主因子，AI 立刻追問最關鍵缺口
    +
信心度評估（強化 C）：
  - 輸出：0–100 數值 + 高/中/低 + 使用模型 + 評估依據（1–2句）
  - 信心不足時說明原因類型：資訊不足 / 描述矛盾 / 證據方向不明
```

**信心度分流（三路）**

| 信心度 | 定義 | 下一步 |
|---|---|---|
| 高 | 因子相關，根源可推 | 進子因子層 |
| 中 | 相關性未定 | 調查建議清單 → QA |
| 低 | 確認不相關 | 魚骨圖保留 + AI 說明理由 |

---

#### 模組三｜風險對應與試驗計畫書

**兩層決策（主因子 → 子因子）**

主因子高信心 → 展開子因子層：

```
AI 四源交叉推論子因子（強化 B）
  輸入：問題描述 + 器材型號 + 510k 驗證項 + 主因子描述
  輸出：2–5 個子因子候選（最多 5 個）

使用者確認 / 修正子因子清單
    ↓
子因子信心度評估（AI 判斷 + 人確認，同主因子邏輯）
    ↓
子因子三路分流：高 / 中 / 低
```

**子因子信心度定義**

| 等級 | 定義 |
|---|---|
| 高 | 問答中有明確描述或量測數據指向此子因子 |
| 中 | 合理懷疑但無法確認，需進一步調查 |
| 低 | 問答中已明確說明不相關或已排除 |

**高度風險驗證項：三合一來源**

```
① 510k 萃取項（競品驗證實績）
② AI 工程判斷（因子特性 → 驗證工具配對）
③ AI 建議 ISO（AI 知識方向，RA 確認版本）
    ↓
使用者確認清單：確認因子正確 + 確認驗證項是否採用
    ↓
驗證項優先排序（強化 D）：AI 給建議執行順序
  Step 1 先做什麼 → Step 2 → Step 3
```

**驗證工具對照表（移除 DOE）**

| 工具 | 適用場景 |
|---|---|
| SPC | 製程穩定性監控，關鍵特性持續監控 |
| MSA / Gage R&R | 量測系統驗證，排除量測誤差 |
| IFU 評估 | 使用端問題，人因工程 62366 |
| 供應商調查 | 來料品質，供應商稽核 |
| 量測方法開發 | 測不到的因子，開發替代指標 |
| 風險文件 | 14971 風險管理，PMS，臨床評估 |

**ISO 13485 建議對應（AI 建議方向）**

| 建議標準 | 情境 | 條款方向 |
|---|---|---|
| 製程確效 IQ/OQ/PQ | 製程穩定性問題 | §7.5.6 |
| 風險管理 14971 | 風險識別與降低 | §7.1 |
| 生物相容性 10993 | 材料相關因子 | §7.5.5 |
| 供應商管控 | 來料品質問題 | §7.4 |
| CAPA 有效性驗證 | 矯正後效果確認 | §8.5.2 |
| 預防措施 | 潛在問題預防 | §8.5.3 |

**試驗計畫書輸出格式**

```
高度風險項：子因子根因 + 驗證三合一 + 優先順序 + 使用者確認紀錄
中度風險項：調查建議清單 + 給 QA 的行動項
低風險項：魚骨圖列出 + AI 說明不相關理由（稽核用）
```

---

### AI 優勢強化四點

| 強化 | 說明 | 使用的 AI 優勢 | 版本 |
|---|---|---|---|
| A：即時追問 | 填一個因子立刻追問缺口，六輪根因訪談 | 語意理解 | v2 |
| B：四源交叉推論 | 問題描述 + 器材型號 + 510k + 主因子描述同時輸入 | 跨領域連結 | v1 |
| C：信心差異分析 | 信心不足時說明原因類型（資訊不足/矛盾/方向不明）| 不確定表達 | v1 |
| D：驗證項優先排序 | 不只清單，給建議執行順序 Step 1→2→3 | 跨領域連結 + 不確定表達 | v2 |

---

### dmaic2_data.json Schema

```json
{
  "session_id": "string",
  "created_at": "ISO8601",
  "device_info": {
    "name": "string",
    "product_code": "string",
    "applicant": "string"
  },
  "verification_items": [
    {
      "source": "510k",
      "item": "string",
      "pdf_ref": "string"
    }
  ],
  "capa_factors": {
    "problem_description": "string",
    "factors": [
      {
        "category": "人|機|料|法|環|測量",
        "description": "string",
        "confidence": { "score": 0, "level": "高|中|低", "model": "string", "reason": "string", "gap_type": "資訊不足|描述矛盾|方向不明|null" },
        "sub_factors": [
          {
            "name": "string",
            "confidence": { "score": 0, "level": "高|中|低", "reason": "string" },
            "risk_level": "高|中|低",
            "verification_items": [
              { "source": "510k|AI判斷|AI_ISO", "item": "string", "priority": 1, "iso_note": "string" }
            ],
            "investigation_items": ["string"],
            "fishbone_reason": "string"
          }
        ]
      }
    ]
  },
  "risk_outputs": {
    "high": [ { "sub_factor": "string", "verifications": [], "user_confirmed": false } ],
    "medium": [ { "sub_factor": "string", "investigation_items": [] } ],
    "low": [ { "sub_factor": "string", "ai_reason": "string" } ]
  },
  "test_plan": {
    "generated_at": "ISO8601",
    "high_risk_section": "string",
    "medium_risk_section": "string",
    "low_risk_section": "string"
  }
}
```

---

### 施工單（DMAIC2 專用）

#### WO-D1（v1 必做）｜四源交叉推論子因子 prompt

```
【目標】把子因子推論的輸入從「主因子名稱」升級為四個來源同時輸入
【輸入】
  - problem_description（使用者填的問題描述）
  - device_info.name + product_code
  - verification_items（510k 萃取項）
  - factors[i].description（該主因子描述）
【輸出】sub_factors 候選清單，最多 5 個，附信心權重
【邊界】不得自行新增 510k 沒有提到且 AI 知識無法支持的子因子
【驗收】同一個問題用主因子名稱 vs 四源輸入，比對子因子相關性差異
```

#### WO-D2（v1 必做）｜信心度差異分析

```
【目標】信心度評估輸出增加 gap_type 欄位
【gap_type 定義】
  - 資訊不足：描述太簡短，無法判斷
  - 描述矛盾：填寫內容與其他因子或問題描述有邏輯衝突
  - 方向不明：資訊存在但指向多個可能原因
  - null：信心度高，無需標示
【prompt 規則】信心 < 60 時必須輸出 gap_type，不得為 null
【驗收】刻意填一個矛盾描述，gap_type 應標「描述矛盾」
```

#### WO-D3（v2）｜即時追問介面

```
【目標】填完一個主因子，系統立刻顯示 AI 追問（不等六個都填完）
【前端】每個因子卡片有「AI 追問」區塊，填完描述後觸發
【後端】prompt：根據此因子描述，找出最關鍵的一個缺口，提出一個追問
【邊界】每個因子最多一個追問，不得連問，避免使用者疲勞
【驗收】填「設備校驗有問題」，追問應該問到「有無 Gage R&R 數據」
```

#### WO-D4（v2）｜驗證項優先排序

```
【目標】三合一驗證清單輸出時附帶 priority 欄位與執行建議
【排序邏輯】
  1. 能釐清根因假設的驗證優先（MSA 先於 SPC）
  2. 成本低的優先（IFU 審查先於供應商稽核）
  3. 有 510k 對照的項目優先
【輸出】verifications 陣列按 priority 排序，附 reason 說明為何這順序
【驗收】給一個量測相關因子，MSA 應排 Step 1
```

---

### 版本路徑

```
模擬版（dmaic2.html）
  v1：模組一 + 六因子表單 + 信心度（強化 B + C）+ 試驗計畫書
  v2：加即時追問（強化 A）+ 驗證排序（強化 D）+ 子因子展開

API 版（dmaic2-worker.js）
  試驗結果 OK 後才轉，不急
  沿用 MedQA 的 Cloudflare Worker 架構，獨立部署
```

---

## 給自己的行動建議

1. **下次遇到新功能需求，先做 OOA**——在紙上寫出「哪幾件事會獨立變化」，再開始對 AI 下 prompt
2. **遇到「改 A 壞 B」的情況，停下來找 Forces**——通常是兩個維度被綁在一起了
3. **選 Pattern 時要說出理由**——「因為這兩個 Force 衝突，所以選 X Pattern 化解」，說不出理由就先別選
4. **MedQA 的 correctNature 重構是最好的練習題**——Forces 已知、問題已知、下一步清楚
5. **怕 AI 品質跑掉時，固定結構讓 AI 填內容**——清單寫死、加上「不要自行新增」，就是品質防線
6. **遇到複雜場景不要追求一次到位**——先做 v1（穩定可控），再用 v2/v3 逐步加複雜度
7. **施工順序看 Force 性質**——先修「不做會爛掉」的（分類、解耦、品質），再加「做了更好」的（關聯、滑動自主）
8. **MedQA 第一波就照實戰篇的 ①～④ 做**——架構打對，後面才能安全疊加
9. **任何 prompt 都從「錨定＋限制」起手**——先想「要 AI 往哪走（錨定）、別往哪走（限制）」，高風險再加思維鏈、自我批判、信心標註
10. **真要動手改 worker.js，先做 WO-7（純加註解、零風險），再照 WO-1 → WO-6 依序**——每張獨立驗收、可回滾，有 quota 時一張一張來
11. **好模組的四條件當檢查表**——單一職責、明確 I/O、不依賴全域、可獨立測試；510k 模組是樣板
12. **check 分兩層、順序不可反**——先程式硬驗（不花 token），過了再 AI 軟驗（必要時才花）

---

*筆記版本：2026-06-27 v9（新增 DMAIC2 設計篇：全流程規格、AI 強化四點、dmaic2_data.json schema、施工單 WO-D1～WO-D4）*  
*參考案例：MedQA（chiuchangru.github.io/DMAIC，worker.js v5.3-b06）｜對照來源：regulatory_ai_guide PART 1*
